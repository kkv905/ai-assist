[project]
name = "ai-assist"
version = "0.1.0"
description = ""
authors = [
    {name = "Krill K.",email = "test@test.ru"}
]
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = [
    "dotenv (>=0.9.9,<0.10.0)",
    "openai (>=2.38.0,<3.0.0)",
    "tenacity (>=9.1.4,<10.0.0)",
    "loguru (>=0.7.3,<0.8.0)",
    "redis (>=7.4.0,<8.0.0)"
]

[tool.ruff]
line-length = 120
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
ignore = []

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pytest (>=9.0.3,<10.0.0)",
    "ruff (>=0.15.12,<0.16.0)"
]